spring:
  datasource:
    url: jdbc:h2:tcp://localhost/~/instaclone
    username: sa
    password:
    driver-class-name: org.h2.Driver

  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
#      show_sql: true
      format_sql: true

  thymeleaf:
    prefix: classpath:/templates/
    suffix: .html
logging.level:
    org.hibernate.SQL: debug
#   org.hibernate.type: trace


#파일경로
file.dir: '/Users/youngcheolseo/Desktop/Spring_study/file/'